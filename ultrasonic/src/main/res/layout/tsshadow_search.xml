<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:a="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical">

    <Button
        android:id="@+id/toggle_filters"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="end"
        android:text="@string/show_filters" />

    <include layout="@layout/tsshadow_filters" />

    <!-- NestedScrollView to allow scrolling inside SwipeRefreshLayout -->
    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fillViewport="true">  <!-- Ensures full expansion -->

        <android.widget.GridLayout
            android:id="@+id/gridLayoutContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:clipToPadding="false"
            android:columnCount="3"
            android:orientation="horizontal"
            android:padding="8dp" />

    </androidx.core.widget.NestedScrollView>

</LinearLayout>